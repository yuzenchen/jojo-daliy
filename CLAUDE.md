# JOJO — 寵物養成日誌 App

給 Claude Code 的專案說明。開發前先讀完本檔。

## 這是什麼

把家犬「JOJO」的日常照顧（飲食、散步、排泄、訓練、照顧、醫療）記錄下來，並以
「電子雞式養成」的介面呈現，讓全家人願意長期維護。

**核心設計原則（不可違反）：**

1. **真實行為驅動數值，不是虛擬需求驅動。** 屬性條全部由真實紀錄推導，不憑空衰減。
2. **只累積、不懲罰。** 漏記一天不扣分，只是那天沒進度。這條決定 App 存活率。
3. **醫療不當扣血，當成資料與提醒。** 疫苗＝倒數提醒與成就；就診＝日誌。生病紀錄
   絕不影響任何遊戲數值——否則狗真的生病時使用者會不想打開 App。
4. **兩層介面。** 日常＝3 秒快速記錄（大按鈕）；養成畫面＝回顧時才看的獎勵。
5. **全家共用，每筆紀錄標記記錄者**（避免重複餵飯）。

## 使用者情境

- 使用者：一家人共用，多人各自記錄。
- 已有 JOJO 的完整病歷／疫苗紀錄要匯入。
- 第一版要求：飲食、醫療、訓練「全部都要」，但陽春可用即可。

## 四條屬性（推導方式，務必照做）

| 屬性 | 來源 | 公式概念 |
|------|------|----------|
| 體態 | 體重 vs 目標體重 | 偏離目標越多分數越低 |
| 活力 | 近 7 天散步分鐘 | 對「每天約 60 分」為滿分正規化 |
| 技能 | 技能樹總進度 | 各技能練習輪數上限加總 |
| 羈絆 | 近 30 天有記錄的天數 | 記錄天數 / 30 |

技能等級門檻（暫定，上線後要依實際訓練頻率調整）：
未開始 0 / 學習中 10 / 熟練 25 / 精通 50 輪。

## 功能範圍

### 第一版（MVP，先做這些）
- [x] 使用者名稱設定（記在本機，用來標記記錄者）
- [x] JOJO 基本資料（名字、生日/到家日、品種、目標體重）
- [x] 快速記錄：吃飯、散步、便便、訓練、照顧、體重
- [x] 養成主畫面：像素狗 + 四條屬性 + 今日記錄狀態 + 連續天數
- [x] 技能樹（基礎 6 項 + 進階 6 項，可調）
- [x] 健康頁：疫苗/驅蟲倒數、體重曲線、就診紀錄
- [x] 月曆印章（一天記滿 3 類蓋實心爪印）
- [x] 病歷匯入（健康頁「批次匯入」：疫苗/就診/體重，每行一筆逗號分隔，含預覽與重複略過）

### 第二版（做成真 App 才有意義的部分）
- [ ] **推播通知**：疫苗到期前 N 天提醒。這是原型做不到、也是升級成真 App 的主要理由。
- [ ] 多人帳號與權限（誰能編輯 vs 只能看）
- [ ] 給獸醫/保母的唯讀分享連結
- [ ] 照片（每餐/每次散步可附一張）
- [ ] 資料匯出（PDF 病歷摘要，回診時給獸醫）

### 明確不做（避免範圍膨脹）
- 不做社群/排行榜/多隻寵物比較。
- 不做付費金流。
- 生病狀態不影響遊戲數值，永遠不加「生病扣血」機制。

## 技術選型建議

沿用原型的技術棧，降低搬移成本：

- **前端**：React + Vite（原型已是 React 單檔，可直接拆分）。
- **樣式**：原型用純 CSS，建議維持，避免引入重型 UI 框架。
- **狀態**：先用 React 內建 state + Context，資料量小不需要 Redux。
- **後端/儲存**：這是重點決策，見下節。
- **像素狗**：原型用 SVG 逐格繪製，可直接沿用。

### 目前狀態（方案 B 已完成——自架後端，全家共用）

- Vite + React 專案，開發跑 `docker compose up`（含 api 服務）。
- **共用資料走自架 API + SQLite**（`server/index.js`，零 npm 依賴，用 Node 24
  內建 `node:sqlite`）。`shared=true` → API；`shared=false`（使用者名字）→ IndexedDB。
- 併發安全：每 key 版本號 + 409 衝突時客戶端合併重試（規則在 `src/lib/storage.js`），
  兩支手機同時記錄不會互相蓋掉。前端每 20 秒與切回前景時輪詢更新。
- 舊純本機資料首次連線自動搬上伺服器（storage.js 內建，一次性）。
- 正式部署：`docker compose -f docker-compose.pi.yml up -d --build`（樹莓派，
  nginx 靜態檔 + API 兩容器，實測合計約 30MB RAM），資料在 `./data/jojo.db`。

### 儲存方案（關鍵決策）

原型用的 `window.storage` 是 Claude 產物環境專屬 API，本專案不存在，已替換。
未來升級路線二選一：

**方案 A — 純本機（最快，適合先驗證）**
- 用 IndexedDB（可透過 Dexie.js 簡化）存所有紀錄。
- 優點：零後端、零成本、離線可用。
- 缺點：資料綁單一裝置/瀏覽器，做不到全家跨裝置共用與推播。
- 適合：先自己用兩三週，確認會長期記錄再升級。

**方案 B — 輕量後端（要達成全家共用 + 推播就走這條）**
- 建議 Supabase 或 Firebase：附帶帳號、即時同步、雲端資料庫。
- 推播用 Web Push（PWA）或包成 App 後用原生推播。
- 缺點：要設定後端、可能有小額費用。

> 建議：**先做方案 A 上線自用，把資料層抽象成一個 `storage` 介面**（get/set/list/delete），
> 之後換成 B 時只改這一層，UI 不動。

### PWA（讓它能「裝」到手機桌面）
- 加 manifest + service worker，讓它能加到主畫面、離線開啟。
- 注意：iOS 的 Web Push 需 iOS 16.4+ 且必須「加入主畫面」後才收得到通知。
  若推播是硬需求，評估是否要包成原生 App。

## 從原型搬移的具體步驟

1. `npm create vite@latest jojo -- --template react`
2. 把原型 `jojo-pet-log.jsx` 拆成元件檔：
   `PixelDog`、`Bars`、`SkillTree`、`Health`、`Calendar`、`RecordSheet`、各表單。
3. 建立 `src/lib/storage.js`，先用 IndexedDB 實作原型那套
   `get/set/list/delete` 介面（保留 `shared` 參數語意，之後接後端才有用）。
4. 把原型的資料鍵（profile / logs / medical / me）對應到新 storage。
5. 資料模型（照原型）：
   - `log`: `{ id, ts, by, type, val, note }`，type ∈ meal/walk/potty/train/care
   - `profile`: `{ name, birth, breed, goalKg, skills: {skillId: reps} }`
   - `medical`: `{ vax: [{id,name,date,cycleDays}], visits: [...], weights: [{id,date,kg}], temps: [{id,date,c}] }`
6. 逐頁驗證後再加第二版功能。

## 環境需求備忘

- 本專案是 Vite app，開發需要 **Node.js（建議 20 LTS 以上）** 才能跑 `npm run dev`。
- Claude Code 工具本身用官方原生安裝器就不需要 Node.js；但你的 App 開發環境仍需要 Node。
- Claude Code 需付費 Anthropic 帳號（Pro / Max / Team / API），免費方案不含。
- 安裝後可跑 `claude doctor` 檢查 Node 版本、網路、認證等環境問題。

## 給 Claude Code 的工作提示

- 動工前先確認上面「核心設計原則」你都理解，特別是「醫療不扣血」與「只累積不懲罰」。
- 一次做一頁、可跑再進下一頁，不要一次改動全部。
- 資料層先抽象，UI 不要直接呼叫 IndexedDB/後端。
- 每個破壞性操作（刪紀錄）要有確認。
- 保留原型的視覺方向（葡萄紫機殼 + 綠色 LCD + 橘色重點色 + 像素狗）。
